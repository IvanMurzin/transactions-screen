---
id: SPEC-XXXX
title: <imperative-mood title under 70 chars>
type: feature  # feature | bug | proposal
status: draft  # draft | open | planned | in_progress | blocked | review | done | closed | rejected
priority: P2   # P0 (critical) | P1 (important) | P2 (normal) | P3 (optional)
owner: <github-handle or "agent">
created: YYYY-MM-DD
updated: YYYY-MM-DD
depends_on: []   # ["SPEC-0003", "BUG-0007"]
blocks: []
related_specs: []
product_docs: []  # ["docs/product/mvp_scope.md#section"]
design_doc: ""    # path to docs/design_system/screens/<area>.md or SPEC-XXXX/design.md
tech_notes: ""    # path to SPEC-XXXX/data-model.md if entity-heavy (optional)
allowed_change_areas: []  # ["client/lib/features/auth/**", "test/features/auth/**"]
forbidden_change_areas: []  # ["backend/supabase/migrations/**", ".env*"]
---

# <Same as frontmatter title>

## Problem
What is broken, missing, or under-specified today? One paragraph, plain
language. Link to product docs that motivate the work.

## Goal
What changes after this spec ships, in one sentence. Frame the goal
from the user's perspective.

## Non-goals
What this spec explicitly does **not** do. Use this section to fence
scope creep early.

---

## User scenarios

Given/When/Then format. Cover the P1 happy path and the most important
edge cases. Label each `US-001`, `US-002` for AC traceability.

**US-001 — <short label>**
- **Given** <precondition>
- **When** <action>
- **Then** <observable outcome>

**US-002 — <short label>**
- **Given** …
- **When** …
- **Then** …

---

## Functional requirements

Numbered `FR-001`, `FR-002` … Unambiguous, testable. Avoid "should" or
"may" unless intentional.

- **FR-001:** …
- **FR-002:** …

---

## Design requirements

> This section is **mandatory** for any spec that adds or changes UI screens.
> Run `/setup-design-system` first if the project DS has not been created.
> Run `ui-ux-pro-max` to generate the design doc and link it in `design_doc`.

**Design doc:** see `design_doc` frontmatter field (generated by `ui-ux-pro-max`).

### Screens and routes
| Screen | Route | Description |
|--------|-------|-------------|
| … | … | … |

### DS components used
List the exact DS components from `client/lib/core_ui/components/`:
- `DSButton` — primary CTA
- `DSCard` — …

### State variants required
For each screen, specify all visual states:
- **Loading** — skeleton / spinner behavior
- **Error** — error message + retry action
- **Empty** — empty state illustration + CTA
- **Populated** — nominal content layout

### Animations and transitions
- Route transition: slide / fade / …
- Loading → populated: …

### Localization keys
All new keys must exist in both `app_en.arb` and `app_ru.arb`:
| Key | EN | RU |
|-----|----|----|
| … | … | … |

---

## Data model

> Use `tech_notes` frontmatter to link a separate `data-model.md` for
> entity-heavy specs. Keep this section for a brief summary.

### Entities
| Entity | Key fields | Notes |
|--------|-----------|-------|
| … | … | … |

### Supabase schema changes
- New table: `…`
- New column: `…`
- New index: `…`

---

## Backend requirements
- New / modified migrations (forward-only, under `backend/supabase/migrations/`).
- New / modified `api_*` RPC functions (SECURITY DEFINER, `p_user_id` pattern).
- New / modified edge function routes (with envelope shape: `{ok, data, meta?}`).
- RLS implications — which roles can read/write what.

---

## Frontend requirements
- New / modified features under `client/lib/{presentation,domain,data}`.
- New / modified DI registrations (`@injectable` / `@lazySingleton`).
- New / modified routes in `app_routes.dart` + `app_router.dart`.

---

## Analytics requirements
Events to log, parameters, when they fire.

| Event | Parameters | Trigger |
|-------|-----------|---------|
| … | … | … |

---

## Security / privacy / RLS requirements
- New PII handled? Where stored, how protected.
- RLS posture changes?
- Secrets touched?

---

## Test requirements

> Tests are **required**. The spec is not done until `flutter test` is clean.

### Unit tests
| Test file | What it covers |
|-----------|---------------|
| `test/presentation/<feature>/<feature>_cubit_test.dart` | All Cubit state transitions |
| … | … |

### Widget tests
| Test file | What it covers |
|-----------|---------------|
| `test/presentation/<feature>/<feature>_page_test.dart` | Smoke test — mounts page, verifies key widgets present |
| … | … |

---

## Acceptance criteria

Each AC is binary: it either passes or fails. Reference user scenarios
(`US-001`) and functional requirements (`FR-001`) where helpful.

- [ ] **AC-1 (US-001):** <observable outcome>
- [ ] **AC-2 (FR-002):** …
- [ ] **AC-3:** `flutter test` passes with no failures.
- [ ] **AC-4:** `flutter analyze` reports zero issues.

---

## Success criteria

Measurable outcomes that confirm the spec delivered value. Reviewed
at `done` status.

- **SC-001:** <metric> reaches <target> within <timeframe>.
- **SC-002:** …

---

## Implementation plan
High-level approach in 5–10 bullets. Pull from `plan_template.md` if
the spec is large enough to warrant a separate plan file.

## Tasks
Optional, only if the implementation has more than ~5 atomic steps.
Use `tasks_template.md`.

## Migration / data requirements
- Backfill needed?
- Idempotency story?

## Rollout notes
Feature flags, ordering with backend deploy, communication.

## Open questions
List with proposed defaults. The spec cannot move past `open` while
this section has unresolved entries.
