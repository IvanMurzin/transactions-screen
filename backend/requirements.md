# Backend Requirements

- Supabase CLI installed and authenticated (`supabase login`).
- `.env` (copied from `.env.example`) filled with `SUPABASE_PROJECT_REF`.
- Optional: `psql` for ad-hoc remote queries.
- Project linked via `supabase link --project-ref <ref>`.
